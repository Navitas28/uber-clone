import React from 'react';
import {View, Text, StyleSheet} from 'react-native';

const EatScreen = (props) => {
	return (
		<View>
			<Text>EatScreen</Text>
		</View>
	);
};

const styles = StyleSheet.create({});

export default EatScreen;
